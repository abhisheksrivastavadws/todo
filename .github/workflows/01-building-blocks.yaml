name: 01 Building Blocks
on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Failed Step
        run: echo "I will fail"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: echo-goodbye
        run: echo "echo goodbye"

