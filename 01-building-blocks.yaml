name: 01 Building Blocks
on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs_on: ubuntu-latest
    steps:
    - name: Failed Step
      echo: "I will fail"
  echo-goodbye:
    runs_on: ubuntu-latest
    steps:
    - name: echo-goodbye
      echo: "echo goodbye"

